# Install with:
#     pip install -r requirements.txt
#
# TA-Lib note (Windows): the pip wheel needs the C library. If
# `pip install TA-Lib` fails, download a matching prebuilt wheel from
# https://github.com/cgohlke/talib-build/releases and install it
# directly, e.g.:
#     pip install TA_Lib-0.4.28-cp311-cp311-win_amd64.whl
# then re-run `pip install -r requirements.txt` to pick up the rest.

PyQt5>=5.15,<6
FinMind>=1.7
requests>=2.28
numpy>=1.23
matplotlib>=3.5
mpl-finance==0.10.1
TA-Lib>=0.4.28
Pillow>=9.0
python-dateutil>=2.8
